[program:{{ app_name }}]
command={{ app_path }}/artisan serve
autostart=true
autorestart=true
user={{ app_user }}
directory={{ app_path }}
redirect_stderr=true
stdout_logfile={{ app_path }}/storage/logs/app.log
environment=PORT="{{ app_port }}",APP_ENV="production"
stopwaitsecs=3600
